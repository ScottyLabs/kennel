# devenv Options

These options are provided by the [shared ScottyLabs devenv module](https://git.cmu.dev/ScottyLabs/kennel). Import it in your `devenv.nix`:

```nix
imports = [ inputs.scottylabs.devenvModules.default ];
```

{{#include generated/devenv-options.md}}
